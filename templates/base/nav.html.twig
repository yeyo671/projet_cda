<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom">
    <div class="container">

        <!-- Brand -->
        <a class="navbar-brand" href="{{path('home')}}">
            <img src="/img/logo.png" class="navbar-brand-img" alt="">
        </a>

        <!-- Toggler -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapse -->
        <div class="collapse navbar-collapse" id="navbarCollapse">
    
          <!-- Toggler -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fe fe-x"></i>
          </button>

        <!-- Navigation -->
        <ul class="navbar-nav ms-auto">
            <li class="nav-item dropdown">
                <a href="{{path('realisations')}}" class="nav-link" href="{{path('realisations')}}">Réalisation</a>
            </li>
            <li class="nav-item dropdown">
                <a href="{{path('actualites')}}" class="nav-link">Actualités</a>
            </li>
            <li class="nav-item dropdown">
                <a href="{{path('portfolio')}}" class="nav-link">Portfolio</a>
            </li>
            <li class="nav-item">
                <a href="{{path('a_propos')}}" class="nav-link">
                    A propos
                </a>
            </li>
        </ul>
        <!-- Button -->
        <a class="navbar-btn btn btn-sm btn-primary lift ms-auto" href="{{path('contact')}}">
            Me contacter
        </a>
    </div>
</nav>